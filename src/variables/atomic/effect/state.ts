import { shadow } from "../color/shadow";

export const state = {
  focus: `0px 0px 0px 2px ${shadow.state.focus}`,
  focusBtn: `0px 0px 0px 2px ${shadow.state.focusBtn}`,
  stroke: `0px 0px 0px 2px ${shadow.state.stroke}`,
  layerBlur: "blur(4px)",
};
